import styled from "styled-components";

const S = {};

  S.LayoutContainer = styled.div`
    display: flex;
    gap: 20px;
    width: 1200px;
    margin: 0 auto;
  `

  S.leftContainer =  styled.div`
  `

  S.RightContainer = styled.div`
    padding-left: 20px;
  `

export default S;